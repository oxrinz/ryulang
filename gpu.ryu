$tensor = $init(10, 10)

fn $tensorAdd(A, B, num_elements) {
    i = block_dim.x * block_idx.x + thread_idx.x
    if (i < num_elements) {
        return A[i] + B[i]
    }
}

$intermediate = $tensorAdd($tensor, $tensor, $tensor.len)

$tensor = $tensorAdd($intermediate, $tensor, $intermediate.len)

host_result = $tensor

print(host_result)
